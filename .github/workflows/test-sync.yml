name: Test Sync Upstream
on:
  workflow_dispatch:
jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Fork
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Sync Upstream (Test)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git remote add upstream https://github.com/nesquena/hermes-webui.git
          git fetch upstream
          git checkout master
          # Only merge if upstream has new commits
          if git diff --quiet upstream/master --exit-code; then
            echo "Already up to date with upstream"
          else
            echo "Would merge upstream changes"
            git merge upstream/master --no-edit
            echo "Merge successful, would push if this had push permission"
          fi